// Data initialization removed - no seed data needed
export async function initializeData() {
  // No initialization needed - users will add their own exercises and templates
}

export async function reloadData() {
  // No reload needed - users will add their own exercises and templates
}

